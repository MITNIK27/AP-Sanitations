// Static product image overrides — keyed by Sanity _id or product name.
// Checked before model.imageSrc (Sanity CDN) and before brand logo fallback.
// Add entries here when images are available locally but not yet uploaded to Sanity.
export const PRODUCT_IMAGES: Record<string, string> = {
  // Sofpour Submersible Transfer Pump (productModel-pnb-kitchenmate-8) — no image yet, uses brand logo fallback
}

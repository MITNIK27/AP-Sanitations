// Static product gallery overrides — keyed by Sanity _id or product name.
// Used on the product detail page when model.gallery[] is empty in Sanity.
// Add entries here when images are available locally but not yet uploaded to Sanity.
export const PRODUCT_GALLERIES: Record<string, string[]> = {}
